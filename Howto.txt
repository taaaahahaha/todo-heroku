pip install virtualenv
virtualenv env
Set-ExecutionPolicy unrestricted (Powershell-Run as admin - [A-Enter])
To activate environment - .\env\Scripts\activate.ps1 [enc tab S tab]
pip install Flask
Create app.py

google-Flask minimal app-copy paste

add two line
if __name__=='__main__':
    app.run(debug=True)

python .\app.py (app-Tab)
[To change port, after debug=True,port=8000]

@app.route("/{name}")
def {name}():
    return "<p>This is {name} page</p>"

make two directories-Static,Templates

Make Templates
index.html
edit first line (app.py)
from flask import Flask, render_template
return render_template('index.html')

getbootstrap.com
components-table/forms[do it inside div.container]
for spacing do my-3
Ex - <div class="container my-3">

Databasethingy
pip install flask-sqlalchemy
in app.py edit- 
from flask_sqlalchemy import SQLAlchemy
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///{name=todo}.db'
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False
db=SQLAlchemy(app)

{sqlite:////tmp/test.db
mysql://username:password@server/db}

class Todo(db.Model):
    sno = db.Column(db.Integer,primary_key=True)
    title = db.Column(db.String(200),primary_key=True,nullable=False)
    desc = db.Column(db.String(500),primary_key=True,nullable=False)
    date_created = db.Column(db.DateTime,default=datetime.utcnow) 

    def __repr__(self) -> str:
       return f"{self.sno} - {self.title}"
       
python interpreter -
python
from app import db
db.create_all()                  [sqllite viewer]-draqanddrop
exit()

Jinja2 snippet kit extension


